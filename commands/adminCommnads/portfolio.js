const { EmbedBuilder } = require("discord.js")
module.exports = {
    name: 'actualizarporfolio',
    description: 'Actualiza portfolio JosueIsOffline',
   //owner: true,
   //options: Object[],
   //testOnly: Boolean,
   //deleted: true,
    callback: async (client, interaction) => {
        
    }
}